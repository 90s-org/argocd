controller:
  serviceAccount:
    create: true
    name: ebs-csi-controller-sa
    # no IRSA role-arn — relies on the EC2 node group's own IAM instance role
    # having EBS permissions attached, same as a bare `helm install` with no overrides
