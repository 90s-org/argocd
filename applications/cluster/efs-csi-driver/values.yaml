controller:
  serviceAccount:
    create: true
    name: efs-csi-controller-sa
    annotations:
      eks.amazonaws.com/role-arn: <your-efs-csi-driver-role-arn>   # from `eksctl create iamserviceaccount ... --role-only`, or a Terraform output
